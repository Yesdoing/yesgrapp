// Imports 

// Actions 

// ActionCreators

// API Actions 

// InitialState
const InitialState = {
    isLoggedIn: false 
}

// Reducer

const reducer = (state = InitialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

// Reduce Functions 

// Export 

// Default Reducer Export 

export default reducer;